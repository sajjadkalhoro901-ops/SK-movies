const search=document.getElementById('search');const cards=Array.from(document.querySelectorAll('.card'));const noResults=document.getElementById('no-results');const count=document.getElementById('movie-count');let activeFilter='all';
function applyFilters(){const value=search.value.toLowerCase().trim();let visible=0;cards.forEach(card=>{const title=card.querySelector('h3').textContent.toLowerCase();const categories=card.dataset.category||'';const categoryMatch=activeFilter==='all'||categories.split(' ').includes(activeFilter);const searchMatch=!value||title.includes(value);const match=categoryMatch&&searchMatch;card.hidden=!match;if(match)visible++});if(count)count.textContent=`${visible} movie${visible===1?'':'s'}`;if(noResults)noResults.hidden=visible!==0}
search.addEventListener('input',applyFilters);document.querySelectorAll('.filter').forEach(button=>{button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(item=>item.classList.remove('active'));button.classList.add('active');activeFilter=button.dataset.filter;applyFilters()})});
document.querySelectorAll('.card img').forEach(img=>{img.addEventListener('error',()=>{if(img.dataset.fallback)return;img.dataset.fallback='true';const label=(img.alt||'SK Movies').replace(/ poster$/i,'').trim();const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="500" height="750" viewBox="0 0 500 750"><rect width="500" height="750" fill="#151515"/><rect x="24" y="24" width="452" height="702" rx="18" fill="#1f1f1f" stroke="#e50914"/><text x="250" y="330" fill="#e50914" font-size="34" font-family="Arial" font-weight="700" text-anchor="middle">SK MOVIES</text><text x="250" y="390" fill="#fff" font-size="22" font-family="Arial" text-anchor="middle">${label.replace(/[&<>\"']/g,'')}</text></svg>`;img.src='data:image/svg+xml;charset=UTF-8,'+encodeURIComponent(svg)})});applyFilters();